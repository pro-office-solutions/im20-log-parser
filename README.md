# im20-log-parser

Simple Cloudflare (Worker + static page) tool to parse IM20 byte arrays into formatted JSON.

## Run locally

```bash
npm install
npm run dev
```

Open the local URL from Wrangler, paste an array of bytes into the textarea, then click **Convert to JSON**.
